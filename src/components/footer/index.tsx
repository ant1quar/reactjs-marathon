import React from 'react';
import cn from 'classnames';
import s from './footer.module.scss';

const Header = () => {
  return <div className={cn(s.footer, 'color')}>FOOTER</div>;
};
export default Header;
